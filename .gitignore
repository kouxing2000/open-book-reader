.DS_Store
*.zip
node_modules/

# Release tooling
.env.chrome-webstore
package/

# Test tooling
test-results/
playwright-report/

# Generated store listing assets (regenerate with `npm run screenshots`)
store-assets/

/design

# Scratch notes — never committed
TODO.md
# screenshots pasted into TODO.md — personal dev notes, keep out of the public repo
# ANCHORED (/image/), or it would also swallow a future site/image/ or store/image/ asset dir
/image/
REFACTOR-PLAN.md
/.deleted

# Internal portfolio/marketing metadata + store metrics — kept locally, not published (RULE #1).
# `.meta` is a gitignored symlink to off-repo private storage; the entry has NO trailing slash
# so it matches the symlink itself (a symlink is not a directory) and can never be committed.
.meta
metrics/

# Claude Code: commit shared project skills/settings (version-controlled, travel with
# the repo), but keep PERSONAL local settings out (RULE #1 covers secrets, not dev docs).
.claude/settings.local.json
